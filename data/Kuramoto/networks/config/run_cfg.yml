---
perform_sweep: True
paths:
  model_note: networks
parameter_space:
  seed: 1
  num_epochs: 0
  write_every: 1
  write_predictions_every: -1
  Kuramoto:
    Data:
      synthetic_data:
        num_steps: 1
        training_set_size: 1
        N: !sweep
          default: 16
          values: [16, 100, 500, 1000]
        network:
          type: random
          mean_degree: !coupled-sweep
            target_name: N
            values: [6, 20, 40, 150]
          graph_props:
            is_directed: False

